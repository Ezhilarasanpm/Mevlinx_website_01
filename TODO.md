- [x] Fix Navbar logo rendering by replacing the stray `alt="MevlinX Technologies"` text with an actual <img> using `logo-dark.png`.

- [ ] Verify there are no other broken logo usages in Navbar (and optionally Footer) affecting layout.
- [ ] Run dev/build to ensure no TS/JSX errors.

